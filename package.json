{
  "private": true,
  "workspaces": {
    "packages": ["frontend", "backend", "smart-contracts"],
    "nohoist": ["smart-contracts", "smart-contracts/**"]
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "wait-on": "^7.2.0"
  },
  "scripts": {
    "dev": "concurrently -k -n chain,api,web -c auto \"cd smart-contracts && yarn start\" \"cd backend && docker compose up --build\" \"wait-on tcp:127.0.0.1:8545 tcp:127.0.0.1:5000 && cd frontend && yarn start\""
  }
}